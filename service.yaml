apiVersion: v1
kind: Service
metadata:
  name: hip-app-service
spec:
  selector:
    app: hip-app
  ports:
    - protocol: TCP
      port: 80       # External port for accessing the app
      targetPort: 8080  # Internal port in the container
  type: LoadBalancer  # Use ClusterIP if you don’t need a load balancer
